#include <iostream>
#include <string>
#include <algorithm>

int main(){
    int n1, n2, t;
    std::cin >> n1 >> n2;
    std::string left, right, lines;

    std::cin >> left >> right;
    std::cin >> t;
    std::reverse(left.begin(), left.end());

    lines = left + right;

    for (int i = 0; i < t; i++){
        
    }

}